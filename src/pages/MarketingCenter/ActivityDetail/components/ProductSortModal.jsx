import React, { useState } from 'react'
import { Modal } from 'antd'
import Drag from '@/components/Drag'

const ProductSortModal = (props) => {
    const { } = props
    return (
        <Modal>
            
        </Modal>
    )
}

export default React.memo(ProductSortModal)